# Railway configuration
# This file tells Railway how to build and deploy your app

[build]
builder = "nixpacks"

[deploy]
startCommand = "NODE_ENV=production SERVE_STATIC=1 npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
